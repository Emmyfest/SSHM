# MongoDB Atlas connection string (create a free cluster at mongodb.com/cloud/atlas)
MONGODB_URI=MONGODB_URI=mongodb+srv://emmyfest643:EMMYfest643%40%23%24@cluster0.3wst9gb.mongodb.net/?appName=Cluster0
DB_NAME=shm

# JWT auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRE_MINUTES=1440

# Bootstrap admin account (created automatically on first startup if no users exist)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# Comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500

# Shared secret devices must send when POSTing readings (simple device auth)
DEVICE_API_KEY=change_this_device_key
